# WeatherAI API key — server-only secret. Copy this file to .env.local and fill
# in a real key. NEVER prefix it with VITE_ / NEXT_PUBLIC_ or it will be inlined
# into the client bundle. .env.local is gitignored.
WEATHERAI_API_KEY=wai_your_key_here
